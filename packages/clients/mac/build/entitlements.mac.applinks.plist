<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Hardened-runtime exceptions Electron's V8 needs. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Dictation — see entitlements.mac.plist. Keep the two files in step:
         this one signs the top-level app, that one every helper. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- Universal links. Requires a Developer ID provisioning profile for
         dev.tella.os1.shell (restricted entitlement) — see README "Universal links".
         The ?mode=developer alternate is listed because os.tella.dev is
         tailnet-only: Apple's AASA CDN can't fetch it, so team devices verify
         the association directly with development mode enabled. -->
    <key>com.apple.developer.associated-domains</key>
    <array>
      <string>applinks:os.tella.dev</string>
      <string>applinks:os.tella.dev?mode=developer</string>
    </array>
  </dict>
</plist>
