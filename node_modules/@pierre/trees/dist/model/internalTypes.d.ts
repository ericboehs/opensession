import { FileTreeIcons } from "../iconConfig.js";
import { GitStatus } from "../publicTypes.js";
import { FileTreeCompositionOptions, FileTreePublicId, FileTreeRenderOptions, FileTreeRowDecorationRenderer, FileTreeScrollOffset, FileTreeSearchBlurBehavior, FileTreeVisibleRow } from "./publicTypes.js";
import { FileTreeController } from "./FileTreeController.js";

//#region src/model/internalTypes.d.ts
type FileTreeControllerListener = () => void;
interface FileTreeStickyRowCandidate {
  row: FileTreeVisibleRow;
  subtreeEndIndex: number;
}
interface FileTreeViewportMetrics {
  itemCount: number;
  itemHeight: number;
  overscan?: number;
  scrollTop: number;
  viewportHeight: number;
}
interface FileTreeRange {
  end: number;
  start: number;
}
interface FileTreeStickyWindowLayout {
  offsetHeight: number;
  stickyInset: number;
  totalHeight: number;
  windowHeight: number;
}
interface FileTreeScrollRequest {
  id: number;
  offset: FileTreeScrollOffset;
  visibleIndex: number;
}
interface FileTreeSlotHost {
  clearSlotContent(slotName: string): void;
  setSlotContent(slotName: string, content: HTMLElement | null): void;
}
interface FileTreeViewProps extends Omit<FileTreeRenderOptions, 'initialVisibleRowCount'> {
  composition?: FileTreeCompositionOptions;
  controller: FileTreeController;
  directoriesWithGitChanges?: ReadonlySet<FileTreePublicId>;
  gitStatusByPath?: ReadonlyMap<FileTreePublicId, GitStatus>;
  ignoredGitDirectories?: ReadonlySet<FileTreePublicId>;
  icons?: FileTreeIcons;
  initialViewportHeight?: number;
  instanceId?: string;
  renamingEnabled?: boolean;
  renderRowDecoration?: FileTreeRowDecorationRenderer;
  searchBlurBehavior?: FileTreeSearchBlurBehavior;
  searchEnabled?: boolean;
  searchFakeFocus?: boolean;
  slotHost?: FileTreeSlotHost;
}
//#endregion
export { FileTreeControllerListener, FileTreeRange, FileTreeScrollRequest, FileTreeSlotHost, FileTreeStickyRowCandidate, FileTreeStickyWindowLayout, FileTreeViewProps, FileTreeViewportMetrics };
//# sourceMappingURL=internalTypes.d.ts.map