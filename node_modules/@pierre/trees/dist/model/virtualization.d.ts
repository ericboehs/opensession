import { FileTreeRange, FileTreeStickyWindowLayout, FileTreeViewportMetrics } from "./internalTypes.js";

//#region src/model/virtualization.d.ts
declare const FILE_TREE_DEFAULT_ITEM_HEIGHT: number;
declare const FILE_TREE_DEFAULT_OVERSCAN = 10;
declare const FILE_TREE_DEFAULT_VIEWPORT_HEIGHT = 420;
declare const EMPTY_RANGE: FileTreeRange;
declare function rangesEqual(left: FileTreeRange, right: FileTreeRange): boolean;
declare function computeVisibleRange({
  itemCount,
  itemHeight,
  scrollTop,
  viewportHeight
}: FileTreeViewportMetrics): FileTreeRange;
declare function computeWindowRange(metrics: FileTreeViewportMetrics, currentRange?: FileTreeRange): FileTreeRange;
declare function computeStickyWindowLayout({
  itemCount,
  itemHeight,
  range,
  viewportHeight
}: {
  itemCount: number;
  itemHeight: number;
  range: FileTreeRange;
  viewportHeight: number;
}): FileTreeStickyWindowLayout;
//#endregion
export { EMPTY_RANGE, FILE_TREE_DEFAULT_ITEM_HEIGHT, FILE_TREE_DEFAULT_OVERSCAN, FILE_TREE_DEFAULT_VIEWPORT_HEIGHT, computeStickyWindowLayout, computeVisibleRange, computeWindowRange, rangesEqual };
//# sourceMappingURL=virtualization.d.ts.map