'use client';


import { CONTEXT_MENU_SLOT_NAME, FILE_TREE_TAG_NAME, HEADER_SLOT_NAME } from "../constants.js";
import { useCallback, useEffect, useLayoutEffect, useMemo, useRef, useState } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";

//#region src/react/FileTree.tsx
const useClientLayoutEffect = typeof window === "undefined" ? useEffect : useLayoutEffect;
function renderFileTreeChildren(header, renderContextMenu, activeContextMenu) {
	const headerChild = header != null ? /* @__PURE__ */ jsx("div", {
		slot: HEADER_SLOT_NAME,
		children: header
	}) : null;
	const contextMenuChild = renderContextMenu != null && activeContextMenu != null ? /* @__PURE__ */ jsx("div", {
		slot: CONTEXT_MENU_SLOT_NAME,
		children: renderContextMenu(activeContextMenu.item, activeContextMenu.context)
	}) : null;
	if (headerChild == null && contextMenuChild == null) return null;
	return /* @__PURE__ */ jsxs(Fragment, { children: [headerChild, contextMenuChild] });
}
function renderPreloadedShadowDom(children, preloadedData) {
	if (typeof window === "undefined" && preloadedData != null) return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("template", {
		shadowrootmode: "open",
		dangerouslySetInnerHTML: { __html: preloadedData.shadowHtml }
	}), children] });
	return /* @__PURE__ */ jsx(Fragment, { children });
}
function hasExistingPreloadedContent(host) {
	const shadowRoot = host.shadowRoot;
	if (shadowRoot?.querySelector("[data-file-tree-id]") instanceof HTMLElement || shadowRoot?.querySelector("[data-file-tree-id]") instanceof SVGElement) return true;
	return host.querySelector("template[shadowrootmode=\"open\"]") instanceof HTMLTemplateElement;
}
function resolveComposition(baselineComposition, header, hasContextMenu, onClose, onOpen) {
	const nextComposition = { ...baselineComposition ?? {} };
	if (header != null) delete nextComposition.header;
	if (hasContextMenu) {
		const baselineContextMenu = baselineComposition?.contextMenu;
		const baselineOnClose = baselineContextMenu?.onClose;
		const baselineOnOpen = baselineContextMenu?.onOpen;
		nextComposition.contextMenu = {
			...baselineContextMenu ?? {},
			enabled: true,
			onClose: () => {
				baselineOnClose?.();
				onClose();
			},
			onOpen: (item, context) => {
				onOpen(item, context);
				baselineOnOpen?.(item, context);
			}
		};
		delete nextComposition.contextMenu.render;
	}
	return nextComposition.header != null || nextComposition.contextMenu != null ? nextComposition : void 0;
}
function FileTree({ header, id, model, preloadedData, renderContextMenu,...hostProps }) {
	const [activeContextMenu, setActiveContextMenu] = useState(null);
	const [hostElement, setHostElement] = useState(null);
	const baselineCompositionRef = useRef(model.getComposition());
	const baselineModelRef = useRef(model);
	if (baselineModelRef.current !== model) {
		baselineModelRef.current = model;
		baselineCompositionRef.current = model.getComposition();
	}
	const hasContextMenu = renderContextMenu != null;
	const handleContextMenuClose = useCallback(() => {
		setActiveContextMenu(null);
	}, []);
	const handleContextMenuOpen = useCallback((item, context) => {
		setActiveContextMenu({
			context,
			item
		});
	}, []);
	const baselineComposition = baselineCompositionRef.current;
	const composition = useMemo(() => resolveComposition(baselineComposition, header, hasContextMenu, handleContextMenuClose, handleContextMenuOpen), [
		baselineComposition,
		handleContextMenuClose,
		handleContextMenuOpen,
		hasContextMenu,
		header
	]);
	const handleHostRef = useCallback((node) => {
		setHostElement(node);
	}, []);
	useEffect(() => {
		if (hasContextMenu) return;
		setActiveContextMenu(null);
	}, [hasContextMenu]);
	useClientLayoutEffect(() => {
		model.setComposition(composition);
	}, [composition, model]);
	useClientLayoutEffect(() => {
		if (hostElement == null) return;
		if (preloadedData != null && hasExistingPreloadedContent(hostElement)) model.hydrate({ fileTreeContainer: hostElement });
		else model.render({ fileTreeContainer: hostElement });
		return () => {
			model.unmount();
			model.setComposition(baselineComposition);
		};
	}, [
		baselineComposition,
		hostElement,
		model,
		preloadedData
	]);
	const children = renderPreloadedShadowDom(renderFileTreeChildren(header, renderContextMenu, activeContextMenu), preloadedData);
	const resolvedHostId = id ?? preloadedData?.id;
	const mergedStyle = {
		["--trees-item-height"]: `${String(model.getItemHeight())}px`,
		["--trees-density-override"]: model.getDensityFactor(),
		...hostProps.style
	};
	return /* @__PURE__ */ jsx(FILE_TREE_TAG_NAME, {
		...hostProps,
		id: resolvedHostId,
		ref: handleHostRef,
		style: mergedStyle,
		suppressHydrationWarning: preloadedData != null,
		children
	});
}

//#endregion
export { FileTree };
//# sourceMappingURL=FileTree.js.map